#include "ChatUI.h"

// Legacy translation unit kept intentionally.
// The actual UI implementations live in the split window modules referenced
// by ChatUI.h; this file ensures older project files that still reference
// ChatUI.cpp continue to build cleanly.
